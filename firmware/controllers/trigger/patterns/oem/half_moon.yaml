name: "Half Moon"
id: TT_HALF_MOON
operation_mode: FOUR_STROKE_CRANK_SENSOR
sync_edge: Rise

channels:
  primary:
    type: skipped_tooth
    total_teeth: 1
    missing_teeth: 0

sync:
  use_only_primary: true

metadata:
  tdc_position: 0.0
  is_crank_based: true
  has_second_channel: false
  known_operation_mode: true

notes: |
  Half moon (180-degree) trigger pattern
  Single tooth per revolution with 180° on, 180° off
  Uses both rising and falling edges for synchronization
